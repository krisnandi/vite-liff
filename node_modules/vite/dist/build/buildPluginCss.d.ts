import { Plugin } from 'rollup';
import { Transform } from '../config';
export declare const createBuildCssPlugin: (root: string, publicBase: string, assetsDir: string, minify?: boolean | "terser" | "esbuild" | undefined, inlineLimit?: number, transforms?: Transform[]) => Plugin;
